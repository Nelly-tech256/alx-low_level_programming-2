#include <stdio.h>

int main(void)
{
	char *char_ptr;

	int* int_ptr;

	printf("Address of the pointer char_ptr is %p\n", &char_ptr);
	 printf("Address of the pointer int_ptr is %p\n", &int_ptr);

	 return (0);
}
