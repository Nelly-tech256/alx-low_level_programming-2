#include <stdio.h>

int main()
{
	int age;

	printf("Enter age :");
	scanf("%d",&age);
	if(age >20 && age <30)
		printf("your age is :%d", age);
		printf("your have grown");
	if(age > 15)
		printf("your age is %d",age);
		printf("its sleeping time");
		return(0);
}

